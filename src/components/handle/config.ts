export const MAX_ATTEMPT_COUNT = 10;
export const HINT_AVAILABLE_ATTEMPT_COUNT = 7; // hints are only available after 7 attempts
export const HANDLE_TIMEOUT_MS = 10 * 60 * 1000; // auto-finish after 5 minutes of inactivity
